---
type: state
module: global
updated: YYYY-MM-DD
---

# Global Changelog

> **Scope: global changes only** — constitution, contracts, tooling, or adding a whole new module.
> Module-scoped changes go in that module's own `<module>/changelog.md`, so parallel contributors never conflict here (constitution rule 4).
>
> Append-only: newest line on top. ADRs and notes are linked from here.
> Line format (example): `YYYY-MM-DD — Memory system installed → [[decisions/YYYY-MM-DD-memory-system-installed|ADR]]`

- (no entries yet — bootstrap adds the first line)
